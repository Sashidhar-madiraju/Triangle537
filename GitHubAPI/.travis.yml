language: python
python:
  - "3.8"
install:
  - pip install -r requirements.txt  # Add this if you have any external dependencies
script:
  - python github_api.py  # Adjust this command if your script has a different filename
